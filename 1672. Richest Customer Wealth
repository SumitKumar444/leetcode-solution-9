class Solution {
    public int maximumWealth(int[][] accounts) {
        int ans=Integer.MIN_VALUE;
        for(int[] num: accounts)
        {
            int sum=0;
            for(int ints: num){
                sum+=ints;
            }
            if(sum>ans)ans=sum;
        }
        return ans;
    }
}
