class Solution {
    public boolean isPalindrome(int x) {
        int sum=0;
        int p=x;
        if(x<0)return false;
        while(x!=0)
        {
            sum=sum*10+ x%10;
            x/=10;
        }
        if(sum==p)return true;
        
        return false;
    }
}
